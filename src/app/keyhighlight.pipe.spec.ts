import { KeyhighlightPipe } from './keyhighlight.pipe';

describe('KeyhighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new KeyhighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
